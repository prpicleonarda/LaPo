{
  "name": "lapo",
  "version": "1.0.0",
  "description": "Order { flower id, email, phone_number, type, delivery, greenery, tape, paper, message, address, time, delivery, gdrp } Flowers { name, color, type, amount, image_link }",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:css": "npx tailwindcss -i ./static/css/tailwind.css -o ./static/dist/tailwind.css --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^3.4.10"
  }
}
